# Anthropic API key (required)
ANTHROPIC_API_KEY=sk-ant-...

# LLM model overrides (optional)
# TRIAGE_MODEL=claude-haiku-4-5-20251001
# SYNTHESIS_MODEL=claude-sonnet-4-20250514

# Comma-separated relevance topics (optional)
# RELEVANCE_TOPICS=AI orchestration,fragrance design,arbitrage/DeFi

# Override recipient email (optional, defaults to authenticated Gmail user)
# RECIPIENT_EMAIL=

# Days to look back on first run (optional, default 7)
# INITIAL_LOOKBACK_DAYS=7

# Max tokens per extraction context (optional, default 4000)
# TOKEN_BUDGET=4000

# Gmail search query (optional, default uses Gmail's importance pre-filter)
# Remove "is:important" if too many newsletters are being skipped
# GMAIL_QUERY=category:updates is:unread is:important
